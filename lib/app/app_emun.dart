
// application loading state
enum LoadState{
  idle,
  loading,
} 

// badge images
enum BadgeImage{
  premium,
  gold,
  diamond,
}

// rest password state
enum ResetState{
  sendOtpMail,
  resetWithOtp,
}

// rest password state
enum EmailValidateState{
  sendOtpMail,
  validateWithOtp,
}